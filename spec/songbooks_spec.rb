require 'spec_helper'

describe Songbooks do
  it 'has a version number' do
    expect(Songbooks::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
